name: caller

on:
  push:
    branches: [ "main" ]

jobs:
  token:
    uses: ./.github/workflows/reusable_build.yml

    with:
      app_name: "my-web-app"
      environment: "production"
    secrets:
      docker_token: ${{ secrets.DOCKER_TOKEN }}
