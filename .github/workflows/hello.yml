name: hello
on:
  push:
    branches: [ "main" ]

jobs:
  greet:
    runs-on: ubuntu:latest

  steps:
    - name: code checkout
      uses: actions/checkout@v5

    - name: print hello
      uses: echo "Hello from GitHub Actions!"
