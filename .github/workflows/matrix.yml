name: matrix_strategy

on:
  push:
    branches: [ "main" ]

jobs:
  matrix_strategy:
  startegy:
    matrix:
      version: [ 10,12,14 ]
      os: [ ubuntu-latest ]

    runs-on: ${{ matrix.os }}

    steps:
       - name: os
         run : echo "cat /etc/os-release"
